# Copy to .env and fill in. Never commit .env (it is gitignored).
GEMINI_API_KEY=
# optional, for cheap text-only stages
GROQ_API_KEY=
CEREBRAS_API_KEY=
# optional, only needed to fetch ICLR PDFs
OPENREVIEW_USERNAME=
OPENREVIEW_PASSWORD=
