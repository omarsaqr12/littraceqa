# Data

Not committed -- CC BY-NC 4.0, fetch from the source:

    bash scripts/download_data.sh

Pulls `paper_metadata.jsonl` (27,487 papers), `validation.jsonl` (55, with gold),
`validation_inputs.jsonl`, `test.jsonl` (71), `test_extra.jsonl` (4,901), the
official `evaluate.py` / `validate_submission.py`, and the JSON schemas from
https://huggingface.co/datasets/LitTraceQA/LitTraceQA
